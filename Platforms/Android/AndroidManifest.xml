<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  xmlns:tools="http://schemas.android.com/tools">
	<uses-sdk android:minSdkVersion="24" android:targetSdkVersion="36" />
	<application android:allowBackup="true"
				 android:supportsRtl="true"
				 android:forceDarkAllowed="false"
				 android:enableOnBackInvokedCallback="false"
				 android:icon="@mipmap/appicon2"
				 android:roundIcon="@mipmap/appicon2_round"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.USE_BIOMETRIC" />
	<uses-permission android:name="android.permission.USE_FINGERPRINT" />
	<!-- The app does not use the Advertising ID. A transitive dependency
	     (Google Play Services) merges this permission; explicitly remove it so
	     Google Play does not flag a missing advertising-ID declaration. -->
	<uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
</manifest>